# Use a RELATIVE path so the database lives inside the project folder
# regardless of where the project is cloned. Prisma resolves this relative
# to the prisma/schema.prisma file location.
DATABASE_URL="file:./db/custom.db"
