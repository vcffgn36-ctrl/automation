# Database URL — Prisma SQLite.
# Use a RELATIVE path so it works on any machine regardless of where the
# project is cloned. Prisma resolves "./db/custom.db" relative to the
# prisma/schema.prisma file location.
DATABASE_URL="file:./db/custom.db"
